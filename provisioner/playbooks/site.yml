- hosts: localhost
  become: no
  vars:
    homebrew:
      packages:
        - tmux
        - peco
        - ghq
        - reattach-to-user-namespace
    homebrew_cask:
      packages:
        - virtualbox
        - virtualbox-extension-pack
        - dockertoolbox

    anyenv:
      - env_name: rbenv
        versions:
          - 2.1.2
          - 2.3.0
        global: 2.3.0
      - env_name: goenv
        versions:
          - 1.6
        global: 1.6
      - env_name: ndenv
        versions:
          - v5.11.0
        global: v5.11.0

  roles:
    - homebrew
    - terminal
    - anyenv
