- hosts: localhost
  become: no
  vars:
    homebrew:
      packages:
        - tmux
        - peco
        - ghq
    homebrew_cask:
      packages:
        - virtualbox
        - dockertoolbox

  roles:
    - homebrew
