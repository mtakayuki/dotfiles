- hosts: localhost
  become: no
  vars:
    homebrew:
      packages:
        - tmux
        - peco
        - ghq
        - reattach-to-user-namespace
    homebrew_cask:
      packages:
        - virtualbox
        - dockertoolbox

  roles:
    - homebrew
    - terminal
